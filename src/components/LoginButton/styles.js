import brown from '@material-ui/core/colors/brown';

const styles = (theme) => ({
  button: {
    margin: theme.spacing.unit,
    width: 200,
    height: 10,
  },
  input: {
    display: 'none',
  },
});

export default styles;
