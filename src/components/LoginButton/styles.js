import brown from '@material-ui/core/colors/brown';

const styles = (theme) => ({
  button: {
    margin: theme.spacing.unit,
    width: '300%',
    height: '40%',
    margin: 40,
    marginTop: '3%',
    marginLeft: '30%',
    marginRight: '40%',
  },
  input: {
    display: 'none',
  },
});

export default styles;
